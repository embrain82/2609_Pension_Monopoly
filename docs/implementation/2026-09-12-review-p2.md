# P2 · 지역 수집과 게임 장면 (v1.8.0)

승인: 2026-09-12 사용자 “P2 스텝도 구현하자.” 기준 main `9b27986`, 작업 브랜치 `codex/p2-v1.8.0`.

대상은 원본 Ego 리뷰의 P2 다섯 항목입니다. F09 → D03 → D08 → D01·D10 순으로 구현하고 서로 연결합니다. 금융 규정·세금·점수·주문·RNG·턴당 납입 200만원·X 취소·성향과 캐릭터 분리는 보존합니다. 매턴 경로 토큰과 보류된 디폴트옵션 후속 4개는 포함하지 않습니다.

| ID | 구현 내용 | 호환·검증 기준 |
|---|---|---|
| F09 | 지역별 서로 다른 두 칸 방문으로 자동 도장. 도장 2/2와 방문 n/6 구분, 도착 시 알림. 이유 남기기는 결과의 선택 복기로 이동. | 새 UI 판 `route.version=auto-v1`만 적용. 구 c1/c2/c3는 기존 수동 획득. 분기에도 규칙 보존. 금액·별·업적 가산 없음. |
| D03 | 기존 동물 SVG 실루엣을 크게 표시, 작은 발 받침·그림자, 최근 세 칸 흔적과 일회 도착 링. | 기존 24칸 좌표·이동 합 유지. 동작 줄이기는 중간 렌더 없이 즉시 도착, 캐릭터 끔은 “나”. 숨김·복원 시 흔적 정리. |
| D08 | 금리 이전→현재와 %p. 실제 보유 손익과 정산의 시장/외부 입출금/매매·정산을 0 기준 금액 막대로 구분. 결과 자산 흐름도 0 시작 축. | 확정 원장만 입력. 구 저장은 없는 분해를 추정하지 않음. 축 끝 금액 명시, 최소 1천원 축, 숫자·기호·텍스트 병행. |
| D01 | 선택 캐릭터·24칸 미니 보드·주사위의 정적 SVG 표지, 기존 미션·캐릭터·동의·시작 동선 정돈. | 추가 설정 단계 없음. 새 렌더러·외부 이미지 요청 없음. 이어하기가 있으면 첫 동작으로 강조. |
| D10 | 미션 결과·별·핵심 이유 → 자산/학습 요약 → 하나의 다음 도전 → 수집 진열장. 상세 복기를 접어 정리. | 기존 미션/별 판정 사용. 새 업적만 별도 표시. 공유·수령 변경·분기·같은 시드·기록 다운로드 유지. |

지역 도장은 한 판의 방문 기록입니다. 연습은 누적 최고 기록·완주 횟수를 추가하지 않습니다. 완주 후 이유는 현재 결과 화면과 다운로드 JSON에만 남으며 새로고침 뒤 결과를 복원하는 기능은 이번 범위에서 추가하지 않습니다. 금융·학습 원문의 법령 검증 범위는 P1과 같습니다.

## 검증

- `npm test`: **53개 파일, 556개 테스트 통과**. 새 검증 14개에 자동 수집·복원·복기·표지·차트·결과와 80개 금융 동등성 경로 포함.
- `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과. 배포 JS gzip 128.95 kB, CSS gzip 19.23 kB.
- 구 c1/c2/c3 복원과 구 수동 도장, 새 자동 판·분기 저장 검증. 80개 시드(4개 시장·3개 미션 조합)에서 자동 도장 유무에 따른 자금·주문·세원·시장 경로·RNG·별·점수 일치.
- Ego Browser TaskSpace 1의 단일 탭 재사용. 실제 클릭·선택·키보드 입력으로 시작 준비·캐릭터 선택·일반 이동·거래·결과를 확인. 반복 턴은 현재 DOM의 활성 버튼을 검사해 클릭 이벤트를 실행했습니다. 게임 상태를 직접 주입하지 않았습니다.
- A 모바일 390×844: `ld8uvj-1agx2j1`, 연금 미션, 코알라/적극투자형. 납입 합계 1,800만원·중위험 옵션 600만원 매수. 12턴 최종 주문 0건, 월 환산 500,264원·별 3개·자동 도장 4/4. 일시금 재선택 시 목표 미달·별 0개로 실제 판정에 맞게 표시.
- B 데스크톱 1440×1000: `1nf8jad-azxaxl`, 물가와의 경주/구매력 미션, 사슴. 12턴 주문 0건, 실질 운용지수 94.4·목표 미달·별 0개·도장 3/4. 최종 UI와 엔진 대조는 `engine-ui-check.json`에 기록.
- 4턴 새로고침 전후 게임 전체 구조가 동일합니다. 최초 문자열 비교는 객체 키 순서 때문에 달라 보여 구조 비교로 재검증했습니다. `resume-check.json`.
- A 9턴 분기부터 12턴까지 연습: 도장 규칙 보존, 완주 수·최고 점수·컬렉션 증가 없음. `practice-check.json`.
- 결과의 지역 이유 선택 후 펼침 상태 유지, 다운로드 JSON에 이유/도장 포함. 실제 플레이에서 발견한 상세 영역 자동 접힘은 `data-preserve-open`을 결과 복기에만 적용해 보완. `disclosure-check.json`.
- Escape 취소→재진입→포트폴리오 왕복은 턴·행동 수·생활자금·IRP 대기자금 보존. `cancel-check.json`. Ego의 자동 스크롤이 고정 X 바 뒤에 버튼을 놓는 경우 키보드 진입으로 확인했습니다.
- 320×568, 390×844, 768×1024, 1440×1000, 844×390에서 가로 넘침·44px 미만 버튼 없음. 이번에는 실제 Ego 화면 PNG 캡처가 정상 작동하여 표지·보드·이동·차트·달성/미달 결과를 시각 점검했습니다. 실제 iOS Safari 기기나 WCAG 전체 인증 검증은 아닙니다.
- 캐릭터 끔의 “나” 표식 1개, 켬의 사슴/코알라, 일반 이동 종료 후 잔상 0개·말 1개, 동작 줄이기 확인. 기존 이동 중 탭 중단·복구 및 연속 동작 회귀 테스트 통과.
- 사용자/운영자 매뉴얼, 공통 학습자료·이전 매뉴얼·검토 HTML의 내부 링크/앵커 검증 오류 0건. 원본 20개와 FB-007의 관리 원장은 전체 완료로 갱신했습니다.

[모바일 표지](2026-09-12-review-p2/cover-mobile.png) · [데스크톱 표지](2026-09-12-review-p2/cover-desktop.png) · [이동 장면](2026-09-12-review-p2/movement-desktop.png) · [자동 도장](2026-09-12-review-p2/stamp-mobile.png) · [달성 결과](2026-09-12-review-p2/result-mobile-success.png) · [미달 결과](2026-09-12-review-p2/result-mobile-missed.png) · [구매력 결과](2026-09-12-review-p2/result-desktop.png)

## 배포

GitHub PR·main 병합·태그 및 Vercel production 검증은 배포 완료 후 이 항목에 기록합니다.
